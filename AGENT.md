# AGENT.md

AI コーディングエージェント（および人間のコントリビューター）がこのリポジトリで作業する際のガイドライン。

## コメント・ドキュメントのルール

### ソースコードのコメント

- ソースコード内のコメント（XML ドキュメントコメントを含む）はすべて**英語**で書く。
- クラス・メソッド（プロパティ・型を含む）の説明は**3行以下**に収める。どうしても
  難しい場合のみ4行以上を許容するが、その場合も長くし過ぎない。
- 網羅的な `<remarks>` より簡潔な要約を優先する。シグネチャから自明でない
  「なぜ」（不変条件・性能上のトレードオフ・注意点）だけを書き、自明な内容は省く。

### ドキュメント（docs/, README など）

- ドキュメント（`docs/` 配下の Markdown、`README.md`、`AGENT.md` を含む）は**日本語のみ**でよい。
  英語との併記は不要。
- ソースコードのコメントは、上記の通り常に英語のみとする。

### プルリクエスト

- PR のタイトル・本文は**日本語のみ**とする。
