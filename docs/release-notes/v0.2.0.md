# v0.2.0 リリースノート（2026-09-01）

M2「フロンティア法フレームワーク」マイルストーン（[docs/PLAN.md](../PLAN.md) §12）の完了リリース。
「スペックを書けば ZDD が自動構築される」という本ライブラリの中核価値が、s–t パス・全域木・
マッチング・基数制約という 4 つの実問題で検証済みの状態になった。

## ハイライト

- `FrontierBuilder.Build` に「スペック」（`IDdSpec<TState>` / `IArrayDdSpec`）を渡すだけで
  ZDD が自動構築される。集合を 1 つも展開しないので、解の個数が `10^24` を超える族でも
  状態の種類の数だけの手間で構築できる
- グラフ問題 API（`Graph` / `FrontierManager`）と、その上に実装した組み込みスペック
  `PowerSetSpec` / `CardinalitySpec` / `LinearConstraintSpec` / `KnapsackSpec` /
  `PathSpec`（s–t 単純パス） / `SpanningTreeSpec` / `ForestSpec` / `MatchingSpec`
- いずれも正しさを検証済み: `PathSpec` は OEIS A007764、`SpanningTreeSpec`/`ForestSpec` は
  Kirchhoff の行列木定理、`MatchingSpec` はパーマネント照合と一致することを確認
- `bench/ZDD.Net.Benchmarks`（BenchmarkDotNet）による代表 10 ケースのベンチ基準値を
  `docs/benchmarks.md` に記録。以降の性能改善 PR はこの数値との相対比較で判定する
- `docs/frontier-guide.md`（フロンティア法ガイド）を新設。コード片は
  `samples/Zdd.FrontierGuide` として実際に動き、CI が毎回実行する
- API はプレリリース扱いで、v1.0 までは破壊的変更があり得る

## フロンティア法フレームワーク（Frontier）

- feat: M2-1 スペックのインタフェース（IDdSpec / IArrayDdSpec / IHybridDdSpec / DdResult）を追加 (#95)
- feat: M2-2 レベル単位の状態表（状態の重複除去）を追加 (#96)
- feat: M2-3 トップダウン幅優先展開（レベル N→1 の構築と BuildOptions）を追加 (#97)
- feat: M2-4 ボトムアップ削減と FrontierBuilder.Build の完成 (#98)

## 組み込みスペック・グラフ問題 API（Specs / Graphs）

- feat: M2-5 基本の組み合わせスペック（PowerSet/Cardinality/LinearConstraint/Knapsack）を追加 (#99)
- feat: M2-6 グラフデータ構造（Graph / Grid / Complete / Cycle / Path）を追加 (#100)
- feat: M2-7 FrontierManager（introduced/forgotten 頂点・mate スロット割当・幅の見積り）を追加 (#101)
- feat: M2-8 PathSpec (SIMPATH)。s–t 単純パス、OEIS A007764 と照合 (#102)
- feat: M2-9 SpanningTreeSpec/ForestSpec (comp array)。Kirchhoff の行列木定理と照合 (#103)
- feat: M2-10 MatchingSpec（マッチング／完全マッチング）を追加 (#104)

## ベンチマーク

- feat: M2-11 ベンチ基準値（BenchmarkDotNet 初版）とフロンティア幅履歴のテストを追加 (#105)

## ドキュメント

- docs: M2-12 v0.2 リリース（frontier-guide.md / README / CHANGELOG / benchmarks 反映）(#106)
- docs: v0.1.0 リリースノートを追加 (#107)

## 基盤・CI

- Release ワークフローを正式版タグ限定・GitHub Release 自動作成に変更 (#94)

## 既知の制約

- `IHybridDdSpec<TScalar>`（スカラ + `int` 配列の複合状態）は契約のみで、
  `FrontierBuilder.Build` のオーバーロードは未対応（v0.3 以降）
- 変数順序（辺順序）の自動最適化は未実装。今のところ利用者が `Graph.WithEdgeOrder` で選ぶ
  （M3 以降の課題）
- API はプレリリース扱いで、v1.0 まで破壊的変更があり得る

**Full Changelog**: https://github.com/wix-diesel/ZDD.Net/compare/v0.1.0...v0.2.0

詳細な変更点は [CHANGELOG.md](../../CHANGELOG.md#020---2026-09-01) を参照。
